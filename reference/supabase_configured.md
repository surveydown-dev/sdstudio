# Check if Supabase credentials are configured

Checks for required environment variables: SUPABASE_URL and
SUPABASE_SERVICE_KEY

## Usage

``` r
supabase_configured()
```

## Value

Logical indicating if Supabase is configured
