# Detect if running in a deployed environment

Checks various indicators to determine if the app is deployed online vs
running locally

## Usage

``` r
detect_deployment_environment()
```

## Value

Logical indicating if deployed
